import { AvatarCounterDirective } from './avatar-counter.directive';

describe('AvatarCounterDirective', () => {
  it('should create an instance', () => {
    const directive = new AvatarCounterDirective();
    expect(directive).toBeTruthy();
  });
});
