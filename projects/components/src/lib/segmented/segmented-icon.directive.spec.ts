import { SegmentedIconDirective } from './segmented-icon.directive';

describe('SegmentedIconDirective', () => {
  it('should create an instance', () => {
    const directive = new SegmentedIconDirective();
    expect(directive).toBeTruthy();
  });
});
