import { InlineCodeDirective } from './inline-code.directive';

describe('InlineCodeDirective', () => {
  it('should create an instance', () => {
    const directive = new InlineCodeDirective();
    expect(directive).toBeTruthy();
  });
});
